{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Common permissions for Yeonsik's Note",
  "windows": ["main", "db-editor"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
